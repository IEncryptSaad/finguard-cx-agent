import { getAdminSummary, getAuditLogs, getConversations, getKnowledge, getTickets } from '@/lib/api';
export default async function Admin(){
  const [data, conversations, tickets, audits, articles] = await Promise.all([getAdminSummary().catch(()=>({})), getConversations().catch(()=>[]), getTickets().catch(()=>[]), getAuditLogs().catch(()=>[]), getKnowledge().catch(()=>[])]);
  return <main className="min-h-screen p-8"><h1 className="text-3xl font-bold">Admin Dashboard</h1><p className="mt-2 text-slate-300">Supervisor console for conversations, tickets, audit review, knowledge CRUD, and settings.</p><div className="mt-8 grid gap-4 md:grid-cols-5">{Object.entries(data).map(([k,v])=><div key={k} className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><p className="text-sm uppercase text-slate-400">{k.replace('_',' ')}</p><p className="mt-2 text-4xl font-bold">{String(v)}</p></div>)}</div>
  <div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 className="text-xl font-semibold">Conversation viewer</h2>{conversations.map(c=><p key={c.id} className="mt-3 text-sm text-slate-300">{c.id} · {c.status}</p>)}</section><section className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 className="text-xl font-semibold">Tickets</h2>{tickets.map(t=><p key={t.id} className="mt-3 text-sm text-slate-300">{t.priority} · {t.summary}</p>)}</section><section className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 className="text-xl font-semibold">Audit log viewer</h2>{audits.slice(-6).map((a,i)=><p key={i} className="mt-3 text-sm text-slate-300">{a.created_at} · {a.event_type}</p>)}</section><section className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 className="text-xl font-semibold">Knowledge management</h2>{articles.map(a=><article key={a.id} className="mt-3 text-sm text-slate-300"><b>{a.title}</b><p>{a.body}</p></article>)}<p className="mt-4 text-xs text-slate-500">CRUD endpoints are available under /api/v1/knowledge.</p></section></div><section className="mt-8 rounded-2xl border border-slate-800 bg-slate-900 p-6"><h2 className="text-xl font-semibold">Settings</h2><p className="mt-2 text-slate-300">AI providers and premium capabilities are disabled-by-default plugins configured via environment variables.</p></section></main> }
