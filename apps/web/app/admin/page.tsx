async function summary(){ try { const base=process.env.NEXT_PUBLIC_API_BASE_URL ?? 'http://localhost:8000'; const r=await fetch(`${base}/api/v1/admin/summary`, {cache:'no-store'}); return r.json(); } catch { return { open_tickets: 0, handoffs: 0, conversations: 0 }; }}
export default async function Admin(){ const data=await summary(); return <main className="min-h-screen p-8"><h1 className="text-3xl font-bold">Admin Dashboard</h1><p className="mt-2 text-slate-300">Operational shell for supervisors, audit review, lifecycle automations, and support queues.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{Object.entries(data).map(([k,v])=><div key={k} className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><p className="text-sm uppercase text-slate-400">{k.replace('_',' ')}</p><p className="mt-2 text-4xl font-bold">{String(v)}</p></div>)}</div></main> }
